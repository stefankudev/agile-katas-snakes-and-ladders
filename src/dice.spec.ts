

describe('dice', () => {
    // dice roll returns number between 1 and 6

    // dice roll number is always positive

    // when dice rolls X, player token can be advanced by X spaces

    // 2000 dice rolls all roll numbers between 1 and 6
});
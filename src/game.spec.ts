describe('game', () => {

    test('player can win the game', () => {
        // TODO:
        expect(false).toBe(true);
    });

    test('100 rolls should ensure player wins the game', () => {
        // TODO:
        expect(false).toBe(true);
    });



});